# Credential-free Fanwaave organization policy profile.
FANWAAVE_ENV_POLICY_STRICT=true
FANWAAVE_PROVIDER_OWNERSHIP_STRICT=true

# The .github repository must not own provider, database, signing, service-role,
# bearer-token, deployment, push-certificate, or cloud credentials.
